[package]
name = "resonators-py"
version = "0.1.0"
edition = "2024"

[lib]
name = "_resonators"
crate-type = ["cdylib"]

[dependencies]
resonators = { path = "../resonators" }
pyo3 = { version = "0.24", features = ["extension-module"] }
numpy = "0.24"
