[package]
name = "resonators"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }
npyz = { version = "0.9.0", features = ["npz"] }

[[bench]]
name = "bank"
harness = false
